# Example Rules

This directory contains example detection rules and a runner configuration to help new users get started with **clickdetect**.

#### Base runner for example

- ./runner.yml

#### Base rule for example

- ./clickhouse/rules/base.yml

#### Example to run

uv run clickdetect -r runner.yml
